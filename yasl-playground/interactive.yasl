#!yasl

#! // drop args

// while (1) { eval(fgets()); }
1 @(read ! eval 1)

// > { = print "\n" print } println
// > "hello" println
//           ^ This doesn't work!
// Func declare in eval loop doesn't work
// because the scope is limited to a loop.
